# Optional. Leave unset to run the editor in self-contained mode (localStorage + base64 only).
# When both are set (or a runtime config.json is deployed), the accounts feature activates.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
